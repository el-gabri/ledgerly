# Convenience install for local development and the deterministic test suite.
# Optional OpenAI and sentence-transformer integrations are exposed as package
# extras so offline users do not download them unnecessarily.
-e .[dev]
